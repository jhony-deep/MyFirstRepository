public class interface_ex implements interface_apex.exapmle{

    public static string main3(){
        string name='This is for the interface exapmle';
        system.debug(name);
        return name;
    }
}